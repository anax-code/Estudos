
package secao04;

/*
 * @author leandromiguel
 */
public class FormulasMatematicas {
    public static void main (String[] args){
        //A = Math.sqrt(x) : Isso significa que a variável A recebe a raiz quadrada de x
        //A = Math.pow(x,y): Isso significa que a variável A recebe o resultado de x elevado a y
        //A = Math.abs(x)  : Isso significa que a variável A recebe o valor absoluto de x
    }
}
